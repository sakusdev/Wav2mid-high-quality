{
  "name": "wav2mid-high-quality",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-runtime": "node scripts/copy-runtime.mjs",
    "dev": "npm run prepare-runtime && vite",
    "build": "npm run prepare-runtime && vite build && node scripts/check-static-assets.mjs --limit-mib=25",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "benchmark:reference": "node scripts/compare-reference.mjs",
    "check:temporary-assets": "node scripts/check-static-assets.mjs --limit-mib=5",
    "deploy": "npm run build && wrangler deploy",
    "deploy:temporary": "npm run build && npm run check:temporary-assets && wrangler deploy --temporary"
  },
  "dependencies": {
    "@spotify/basic-pitch": "1.0.1",
    "@tensorflow/tfjs": "4.21.0",
    "@tensorflow/tfjs-backend-wasm": "4.21.0",
    "@tensorflow/tfjs-backend-webgpu": "4.21.0",
    "@tonejs/midi": "2.0.28",
    "demucs-web": "1.0.2",
    "fft.js": "4.0.4",
    "onnxruntime-web": "1.27.0"
  },
  "overrides": {
    "@spotify/basic-pitch": {
      "@tensorflow/tfjs": "4.21.0"
    }
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "vite": "5.4.14",
    "wrangler": "4.121.0"
  }
}
